global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "sqlite_exporter"
    static_configs:
      - targets: ["db-exporter:9235"]
        labels:
          instance: "pkpkdupr-db"
