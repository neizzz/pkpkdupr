#!/usr/bin/env sh

set -eu

ROOT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)

cd "$ROOT_DIR"

echo "[pre-push] 전체 빌드 검증을 시작합니다."

if pnpm verify:pre-push; then
  echo "[pre-push] 전체 빌드 검증이 완료되었습니다."
  exit 0
fi

echo "[pre-push] build 실패로 push를 차단합니다." >&2
exit 1
